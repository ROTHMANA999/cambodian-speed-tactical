[build]
  # Use npm to run the build script defined in package.json
  command = "npm run build"
  # TanStack Start / Vite produces client assets under dist/client
  publish = "dist/client"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200